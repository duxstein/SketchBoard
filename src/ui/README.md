# UI Module

**Purpose**: Minimal UI chrome that serves the canvas.

This module contains:

- Toolbar components
- Minimal panels/overlays
- Canvas container wrapper

**Key Principle**: UI exists to serve the canvas, not the opposite. Minimal chrome, keyboard shortcuts prioritized, no modal-heavy workflows (PRD Section 8).
