/**
 * Shape Types
 *
 * Type definitions for shapes (pen, rectangle, ellipse, text, etc.).
 * Must include: id, type, geometry, style, metadata (createdBy, createdAt, updatedAt).
 * Must be immutable, serializable, CRDT-compatible, deterministically renderable.
 */
